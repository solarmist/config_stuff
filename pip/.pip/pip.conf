[global]
require-virtualenv = false
# Homebrew / distro Python is externally managed (PEP 668); allow installs
# but keep them in the user site by passing --user explicitly on the command.
break-system-packages = true